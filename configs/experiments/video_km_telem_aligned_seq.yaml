# Three-modality fusion: video + km + telem (aligned sequence)
_base_: ./video_km_aligned_seq.yaml

data:
  modalities: [video, km, telem]

model:
  encoders:
    telem:
      name: stat_pool
      feature_dim: 109
